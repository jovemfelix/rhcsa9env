---
- name: Enable IPv6 (lab default)
  copy:
    dest: /etc/sysctl.d/99-lab-ipv6.conf
    content: |
      net.ipv6.conf.lo.disable_ipv6 = 0
      net.ipv6.conf.all.disable_ipv6 = 0
    mode: "0644"

- name: Apply sysctl settings
  command: sysctl --system
  changed_when: false
