---
- name: Disable IPv4 forwarding (reset)
  copy:
    dest: /etc/sysctl.d/99-lab-ipforward.conf
    content: |
      net.ipv4.ip_forward = 0
    mode: "0644"

- name: Apply sysctl settings
  command: sysctl --system
  changed_when: false
